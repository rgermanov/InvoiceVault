version: 1
applications:
  - appRoot: frontend
    frontend:
      buildPath: / # Run install and build from the monorepo project root
      phases:
        preBuild:
          commands:
            - npm ic
        build:
          commands:
            - npm run build
      artifacts:
        files:
            - '**/*'
        discard-paths: yes
        baseDirectory: public